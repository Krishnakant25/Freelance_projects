fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
numpy==1.26.4
sentence-transformers==3.1.1
requests==2.32.3
pypdf==6.16.1

# Optional — only needed for VECTOR_INDEX_BACKEND=hnsw (past ~200k chunks).
# See DEPLOYMENT.md §2: the reranker, not vector search, is the bottleneck
# below that scale, so this is deliberately not a default dependency.
# faiss-cpu==1.15.0
