fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
numpy==1.26.4
sentence-transformers==3.1.1
requests==2.32.3

# Test-only
httpx==0.27.2

# NOT installed by default — telephony transport. See README "Honest scope":
# the Twilio adapter is written but UNVERIFIED (no account available).
# twilio==9.3.0
